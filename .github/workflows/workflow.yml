name: CI Pipeline

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Install dependencies
        run: pip install flake8 nose flask

      - name: Lint with flake8
        run: flake8 .

      - name: Run unit tests with nose
        run: nosetests
